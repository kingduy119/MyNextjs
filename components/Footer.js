import React from "react";
import { Container, Row, Col } from "react-bootstrap";
import { footerContainer } from "../lib/stylesheet";

function Footer() {
    return (
        <Container style={footerContainer}>
            <h2>Footer</h2>
        </Container>
    );
};


export default Footer;
